한글을 UTF로 변환
기본적으로 ASCII 127자까지는 모두 사용
이후로 특수문자 처리

font_to_py의 형태로 진행
extended파일로
utf8코드, 순서, utf8코드, 순서
\x45\x48\x7B \xFF\xFF \xFF\xFF => \x\x00\x00............\x00
---------------- ---------- ----------     --------------------------
한글                 순서       개수             비트맵문자데이터

예) '가'라는 글자가 145번째에 등록되어 있고 가로세로24개의bit(3byte * 24 = 72byte = 72개)로 만들어져있을때
인덱스 =>\xEA\xB0\x80\x00\x81\x00\x48,
데이터 => .....\x00\x01\x01....\x00이 72byte